$schema: "https://json-schema.org/draft/2020-12/schema"
description: an entry in the sample sheet
properties:
    sample:
        type: string
        description: sample name/identifier
    lane:
        type: string
        description: sequencing lane of the fastq file
required:
    - sample
    - lane
